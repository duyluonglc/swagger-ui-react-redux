export default {
  useFixtures: false,
  ezLogin: false,
  yellowBox: process.env.dev || 1,
  reduxLogging: process.env.dev || 1,
  includeExamples: process.env.dev || 1,
  useReactotron: process.env.dev || 1
}
